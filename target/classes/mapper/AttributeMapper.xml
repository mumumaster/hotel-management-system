<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gx.dao.AttributeDao" >
  <resultMap id="BaseResultMap" type="com.gx.po.AttributePo" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Oct 16 19:18:30 CST 2016.
    -->
    <constructor >
      <idArg column="id" jdbcType="INTEGER" javaType="java.lang.Integer" />
      <arg column="attributeName" jdbcType="VARCHAR" javaType="java.lang.String" />
    </constructor>
  </resultMap>

<!--   查看客房等级 -->
  <select id="selectGuestRoomLevel" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=2
  </select>
 
 <!--   查看房间状态 -->
  <select id="selectRoomState" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=1
  </select>
  
   <!--   查看商品类别 -->
  <select id="selectCommodityType" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=3
  </select>
  
   <!--   查看计量单位 -->
  <select id="selectUOM" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=15
  </select>
  
  <!--   查看对象类别 -->
  <select id="selectTargetType" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=14
  </select>
  
  <!--   查看性别 -->
  <select id="selectGender" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=8
  </select>
  
  <!--   查看民族 -->
  <select id="selectNation" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=9
  </select>
  
  <!--   查看旅客级别 -->
  <select id="selectPassengerLevel" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=13
  </select>
  
  <!--   查看文化程度 -->
  <select id="selectEducationDegree" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=11
  </select>
  
  <!--   查看证件类型 -->
  <select id="selectPapers" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=10
  </select>
  
  <!--   查看事由 -->
  <select id="selectThingReason" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=12
  </select>
  
  <!--   查看旅客类别-->
  <select id="selectPassengerType" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=7
  </select>
  
  <!--   查看结账单位 -->
  <select id="selectBillUnit" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=6
  </select>
  
  <!--   查看结账方式 -->
  <select id="selectPayWay" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=4
  </select>
  
  <!--   查看出租方式 -->
  <select id="selectRentOutType" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=5
  </select>
  
  <!--   查看结账否 -->
  <select id="selectIsPay" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=17
  </select>
  
  <!--   查看预订状态 -->
  <select id="selectPredetermineState" resultType="com.gx.po.AttributePo" >
    SELECT a.*,b.id AS far_id,b.attributeDetailsName FROM attribute a INNER JOIN attributedetails b ON a.id =b.attributeID WHERE a.id=16
  </select> 
  
  
  
  
  
  
  
  
  
  <!--   新增商品类别 -->
  <insert id="insertAll">
     INSERT INTO attributedetails (attributeID,attributeDetailsName)
     values (#{newid,jdbcType=INTEGER},
             #{newname,jdbcType=VARCHAR})
  </insert>
  
  <!--   删除商品类别 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
    DELETE FROM attributedetails WHERE id = #{id,jdbcType=INTEGER}
   </delete>
  
</mapper>